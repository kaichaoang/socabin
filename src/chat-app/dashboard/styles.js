import defBg from "../../Images/4k-wallpaper-android-wallpaper-astro-astrology-1146134.jpg";

const styles = (theme) => ({
  wrapper: {
    backgroundImage: `url(${defBg})`,
    backgroundSize: "cover",
    height: "100vh",
    width: "100vw",
  },
});

export default styles;
